// import and export all adapters
const cache = require('./cache')
const repository = require('./repository')
const requests = require('./requests')

module.exports = {
    cache,
    repository,
    requests
}